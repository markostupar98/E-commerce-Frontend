import React from "react";
import "./Map.css";

const Map = () => {
  return (
    <div className={`map ${props.className}`} style={props.style}>
      Product details
    </div>
  );
};
export default Map;
